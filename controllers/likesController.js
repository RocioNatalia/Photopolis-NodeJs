const likesGet = (req ,res) => {
    res.json({response : 'likes del usuario'})
}

const likesSet = (req ,res) => {
    res.json({response : 'likes del usuario'})
}

module.exports = {
    likesGet ,
    likesSet
}